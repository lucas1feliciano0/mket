import React from 'react';

import {Container, StatusBar} from './styles';

const Home: React.FC = () => {
  return (
    <Container>
      <StatusBar />
    </Container>
  );
};

export default Home;
